

const initialState = {};

export const activeSongReducer = (state = initialState, action) => {
  switch(action) {
    // case value:
      
    //   break;
  
    default:
      return state;
  }
};
